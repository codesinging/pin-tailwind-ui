# Icon 图标

图标组件可以快速展示一个 `svg` 格式的图标，可以灵活的设置大小、颜色和线条粗细。

## 基本用法

<demo name="Icon/Base"/>

## 图标大小

- 使用 `size` 属性
- 使用 `class` 设置
- 使用 `style` 设置

<demo name="Icon/Size"/>

## 图标颜色

- 使用 `color` 属性
- 使用 `class` 设置
- 使用 `style` 设置

<demo name="Icon/Color"/>

## 线条粗细

使用 `stroke-width` 属性来设置图标的线条粗细

<demo name="Icon/Stroke"/>

## 其它属性

使用 `TailwindCSS` 的工具类设置图标的其它属性

<demo name="Icon/Class"/>
