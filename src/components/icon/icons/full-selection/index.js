import PinIconFullSelection from "./PinIconFullSelection.vue";

PinIconFullSelection.install = Vue => {
    Vue.component(PinIconFullSelection.name, PinIconFullSelection)
}

export default PinIconFullSelection
